(require 'asdf)

;;; The following lines added by ql:add-to-init-file:
#-quicklisp
(let ((quicklisp-init #p"/home/redline/quicklisp/setup.lisp"))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))

(push "/home/redline/projects/systems/" asdf:*central-registry*)
(push "/home/redline/emacs/site-lisp/slime/" asdf:*central-registry*)
