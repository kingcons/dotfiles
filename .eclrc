(require 'asdf)

;;; The following lines added by ql:add-to-init-file:
#-quicklisp
(let ((quicklisp-init #p"/media/redlinux/home/redline/Desktop/quicklisp/setup.lisp"))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))

(push "/home/redline/projects/systems/" asdf:*central-registry*)

(defmacro full-build (system &body body)
  `(asdf:make-build ,system :type :program :monolithic t :move-here t
                    :epilogue-code ',@body))

